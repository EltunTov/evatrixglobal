import styles from "../dashboard.module.css";

export default function AccessPage() {
  return (
    <section className={styles.placeholderPage}>
      <h2>Access & Plan</h2>
      <p>
        This section will later contain feature permissions,
        plan limits, and upgrade path visibility.
      </p>
    </section>
  );
}